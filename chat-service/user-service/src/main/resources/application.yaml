spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/teamsync_users
    username: postgres
    password: postgres123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8081

jwt:
  secret: YourSuperSecretKeyForTeamSyncAI2026MustBe256BitsLong!!
  access-token-expiration: 900000
  refresh-token-expiration: 604800000
